HRRP No-Penalty Rate (%) = 
DIVIDE(
    CALCULATE(COUNTROWS(provider_specific_file), provider_specific_file[hrrParticipantIndicator] = "0"),
    CALCULATE(COUNTROWS(provider_specific_file), provider_specific_file[hrrParticipantIndicator] IN {"1", "0"})
)